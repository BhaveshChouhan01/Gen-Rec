

# Leave blank to force DuckDuckGo fallback (no sign-up needed)
UNSPLASH_ACCESS_KEY= x1KRSyhBTNaSFH-JQzYy5KGdbvadvWhIDtPlA_FHRqA

# Models (defaults are small/fastish)
# Optional: Choose BLIP base or large (large is slower)
BLIP_MODEL=Salesforce/blip-image-captioning-base

# QA model (small/fast)
QA_MODEL=distilbert-base-cased-distilled-squad
# Optional Unsplash free dev key (for nicer search results).
# Leave empty to use DuckDuckGo fallback with no sign-up.

# Max upload size (MB)
MAX_IMAGE_MB=10
